version: 2
jobs:
  build:

    steps:
      - checkout
      - run: 
          name: install build deps
          command: |
              sudo apt-get update
              sudo apt-get install -y libcsv-dev build-essential
              zef install --deps-only .
      - run:
          name: test 
          command: |
              zef test .
